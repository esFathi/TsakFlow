# @taskflow/configs

Shared tooling configuration for the monorepo (base `tsconfig`, and future
shared eslint/prettier presets). Extend from a workspace:

```jsonc
// tsconfig.json
{ "extends": "@taskflow/configs/tsconfig.base.json" }
```
