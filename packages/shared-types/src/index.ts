// Single source of truth for domain types shared between `web` and `api`.
// Add shared entities here (e.g. User, Project, Task) and consume them via
// `@taskflow/shared-types` from either app.

export {}
